import { SearchInputParameter } from './search-input-parameter';

describe('SearchInputParameter', () => {
  it('should create an instance', () => {
    expect(new SearchInputParameter()).toBeTruthy();
  });
});
